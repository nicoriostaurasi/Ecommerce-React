import React from 'react';

const CartWidget = () => {
  return (
    <div>
      <button type="button" className="btn text-light" onClick={() => {
        alert('TBD, next steps');
      }}>🛒 6</button>
    </div>
  );
};

export default CartWidget;
